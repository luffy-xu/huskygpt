Here is a simple example of how to write a "Service":

import fetch from 'lib/fetch';

const mockVariable = 'mockVariable';

export const getVariable = async (idCode: string): Promise<string> => {
  {use mock data}
  const res = mockVariable;
  // const res = await fetch(`/api/${idCode}`);
  {here is adapter(res) code, if necessary}
  return res;
}
